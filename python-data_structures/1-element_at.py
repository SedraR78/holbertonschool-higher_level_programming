#!/usr/bin/python3
def element_at(my_list, idx):
    len(my_list)
    for i in my_list:
        if idx < 0:
            return None
        if idx > len(my_list):
            return None
        elif idx > 0 and idx < len(my_list):
            return idx + 1
